# Leave blank to use the Vite dev proxy (see vite.config.js), which
# forwards /analyze to http://127.0.0.1:8000 during local development.
# Set this if the backend is hosted elsewhere.
VITE_API_BASE_URL=
